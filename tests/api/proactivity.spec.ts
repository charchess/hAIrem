import { test, expect } from '../support/fixtures';

test.describe('Home Proactivity (Epic 15)', () => {
  test('should trigger agent reaction on World State change @p1', async ({ }) => {
    // Simulation d'un changement global (ex: Météo -> Pluie)
    // Vérifie que les agents réagissent proactivement dans le stream.
  });
});