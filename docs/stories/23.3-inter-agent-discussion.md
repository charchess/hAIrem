# Story 23.3 — Flux de Discussion Inter-Agents

## Status: Done

## Constat

Comportements déjà implémentés dans `main.py` + `arbiter.py` et testés dans `test_18_4_inter_agent_discussion.py` :
- Budget décrémenté à chaque échange inter-agent
- Budget réinitialisé à `MAX_DISCUSSION_BUDGET` (5) sur message utilisateur
- Cascade stoppée quand `discussion_budget == 0`
- Threshold dynamique croissant avec les tours (0.75 → 0.95)
- Decay de l'intérêt par tour (`_compute_discussion_decay`)
- Arbiter retourne `None` quand scores < threshold → pas de cascade

## Ajouts story 23.3

Deux tests manquants du sprint plan ajoutés à `test_18_4_inter_agent_discussion.py` :

- `test_arbiter_low_score_stops_cascade` — budget > 0 mais arbiter retourne None → redis.publish jamais appelé
- `test_arbiter_returns_none_when_all_scores_below_threshold` — score LLM = 0.2 < threshold 0.75 → arbiter retourne None

## Total tests

12 tests dans `test_18_4_inter_agent_discussion.py` (10 existants + 2 ajoutés)
