# Story 6.3: Slash Commands (Direct Control)

**Status:** Done

## Acceptance Criteria
1.  [x] L'interface de chat détecte le préfixe `/`.
2.  [x] Si un message commence par `/`, il est converti en type `expert.command`.
3.  [x] Le routage direct vers l'agent cible est fonctionnel via Redis.
4.  [x] Validation : La commande `/Expert-Domotique echo` fonctionne sans passer par le LLM.

## Dev Agent Record
### Agent Model Used
James (Dev) - Gemini 2.0 Flash

### Completion Notes List
- Implemented slash command parsing in `network.js` and `chat_cli.py`.
- Verified direct routing to specialized agents.
- Successful E2E test with the `echo` command bypassing the LLM.

## QA Results
### Review Date: 2026-01-21
### Reviewed By: Quinn (Test Architect)
### Code Quality Assessment
Le mécanisme de bypass du LLM pour les commandes directes est crucial pour la performance et la fiabilité. Le test E2E via `chat_cli.py` confirme la solidité du routage Redis.

### Gate Status
Gate: PASS → docs/qa/gates/6.3-slash-commands.yml