# hAIrem Design & Assets Standards

**Version:** 1.0  
**Author:** Sally (UX Expert)  

---

## 1. Core Poses
Mapping obligatoire : `idle`, `thinking`, `happy`, `sad`, `alert`, `emergency`, `confused`, `shy`, `angry`, `glitch`.

---

## 2. Charte Sonore
Organique "Cyber-Cozy" (bois, tissu, vent). Sons système (`sfx_chime`, `sfx_static`, `sfx_typewriter`).

---

## 3. VFX Registry
`thinking` (dots), `happy` (sparkles), `alert` (pulse), `glitch` (scanlines).

---

## 4. Synchro A/V
Pose Anticipation (200ms), Lip-sync par amplitude, Glow sync.
